# ml-strategy-dev
Inspired by content of "Advances in Financial Machine Learning"

## Input
When importing a new dataset from databento extract it into the raw_data folder

Run convert_to_parquet.py but make sure to change the file name at the top of the script

Run create_database to create the duckdb database for easy querying

